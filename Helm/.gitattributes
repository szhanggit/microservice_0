# Force LF regardless of core.autocrlf - these scripts run under WSL/Linux
# bash, and a CRLF shebang line ("#!/usr/bin/env bash\r") fails with
# "/usr/bin/env: 'bash\r': No such file or directory".
*.sh text eol=lf
justfile text eol=lf
