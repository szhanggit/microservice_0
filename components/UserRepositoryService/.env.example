# Copy this file to .env before running `docker compose up`.
# .env is gitignored - never commit real credentials.

MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=userdb
MYSQL_USER=userdb_app
MYSQL_PASSWORD=changeme
